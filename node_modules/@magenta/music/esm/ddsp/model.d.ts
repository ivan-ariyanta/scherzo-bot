import '@tensorflow/tfjs-backend-webgl';
declare class DDSP {
    private initialized;
    private checkpointUrl;
    private model;
    private settings;
    constructor(checkpointUrl: string, settings?: any);
    initialize(): Promise<void>;
    dispose(): void;
    isInitialized(): boolean;
    memCheck(): Promise<boolean>;
    synthesize(audioFeatures: any, settings?: any): Promise<Float32Array>;
}
export { DDSP };
